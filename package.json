{
  "name": "project",
  "version": "1.0.0",
  "description": "CAP Project",
  "private": true,
  "scripts": {
    "start": "cds run",
    "watch": "cds watch",
    "build": "cds build",
    "deploy": "cds deploy"
  },
  "dependencies": {
    "@sap/cds": "8.9.8",
    "@sap/xssec": "^4",
    "express": "^4",

    "@cap-js/sqlite": "1.4.0",
    "sqlite3": "^5.1.7",

    "@cap-js/hana": "1.9.2"
  },
  "devDependencies": {
    "@sap/cds-dk": "8.9.8"
  },
  "cds": {
    "requires": {
      "auth": {
        "[development]": {
          "kind": "mocked"
        },
        "[production]": {
          "kind": "xsuaa"
        }
      },
      "db": {
        "[development]": {
          "kind": "sqlite"
        },
        "[production]": {
          "kind": "hana"
        }
      }
    }
  }
}


